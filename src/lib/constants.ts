/** Built-in default Public API base URL (resolution order step 4). */
export const DEFAULT_BASE_URL = "https://public-api.textmine.com";

/** The API version segment the CLI always targets. */
export const API_VERSION = "v3";

/** Name of the default profile used when none is specified. */
export const DEFAULT_PROFILE = "default";

/** Environment variable that overrides the resolved base URL. */
export const ENV_BASE_URL = "TEXTMINE_BASE_URL";

/** Environment variable that overrides the resolved API key. */
export const ENV_API_KEY = "TEXTMINE_API_KEY";

/** Environment variable that overrides the active profile. */
export const ENV_PROFILE = "TEXTMINE_PROFILE";

/** Prefix every TextMine API key carries. */
export const API_KEY_PREFIX = "tm_";
